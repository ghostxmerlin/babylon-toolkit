{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "@services/vault",
  "sourceRoot": "{projectRoot}/src",
  "projectType": "application",
  "tags": ["type:service"],
  "targets": {
    "dev:watchDeps": {
      "dependsOn": ["^build"],
      "executor": "nx:run-commands",
      "options": {
        "cwd": "services/vault",
        "parallel": true,
        "commands": [
          {
            "command": "./scripts/watch-deps.sh {projectName}"
          },
          { "command": "pnpm exec vite" }
        ]
      }
    }
  }
}
